---
## CUSTOMIZED FILE
## Feed parentPage and short_title to pageHeader to support better PDF running heads
##
classes:
  - quire-page
layout: base.11ty.js
description: Single page default template
---

{% pageHeader
  byline_format=byline_format,
  image=image,
  label=label,
  pageContributors=pageContributors,
  parentPage=parentPage.data,
  short_title=short_title,
  subtitle=subtitle,
  title=title,
  page_pdf_output=page_pdf_output,
  key=key,
  outputs=outputs
%}
{% if content %}
<section class="section quire-page__content">
  <div class="container">
    <div class="content">
      {{ content }}
      {% bibliography citations outputs page_pdf_output %}
    </div>
  </div>
</section>
{% endif %}
<section class="section quire-page__content">
  <div class="container">
    {% pageButtons pagination=pagination %}
  </div>
</section>
